# Backend

Suggested backend modules:
- `api/` for FastAPI routes
- `database/` for shared DB client and schemas
- `planner/` for orchestration
- `demand/` for forecasting agent
- `replenishment/` for order recommendation agent
- `supplier_risk/` for vendor risk analysis
- `narrator/` for report writing
