[project]
name = "supplychain-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.1",
  "mangum>=0.19.0",
  "python-dotenv>=1.1.1",
  "supplychain-database",
  "uvicorn>=0.35.0",
]

[tool.uv.sources]
supplychain-database = { workspace = true }
